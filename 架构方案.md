# BadNorthHeroTraits 架构方案

## 概述

将原本单 DLL 多特质的 PlentyTraits Mod 拆分为 9 个独立的特质 Mod + 1 个公共 API 库 (BadNorthAPI)。每个 Mod 遵循 **单一职责** 原则，仅负责一个特质的注册与运行逻辑。

5 个特质中：BadNorthFlyer 和 BadNorthTitan 为忠实还原 PlentyTraits 魔改版；BadNorthUltimateSquad、BadNorthYuri、BadNorthSlash 为忠实还原 FancyTraits 魔改版。

## 项目结构（当前实际）

```
BadNorthHeroTraits/                    # 根目录（Git 仓库）
├── 架构方案.md                         # 本文件
│
├── BadNorthAPI/                       # 公共 API 层（前置依赖）
│   └── BNAPI/
│       ├── CustomSprites.cs           # 精灵/图标加载
│       ├── CustomText.cs              # 本地化术语注入
│       ├── CustomTraits.cs            # 特质注册
│       ├── IgnoresAccessChecksToAttribute.cs
│       ├── Debugger.cs                # 调试日志工具
│       ├── Plugin.cs                  # BNAPI 入口（GUID: nacu.bnapi.modular）
│       ├── Properties/AssemblyInfo.cs
│       └── BNAPI.csproj (AssemblyName: BadNorthAPI)
│
├── BadNorthAxeThrower/                # 特质: 掷斧手
│   ├── AxeThrower.cs                  # 特质定义（三级容错获取模板）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_axethrower.png  # 特质图标
│   └── BadNorthAxeThrower.csproj
│
├── BadNorthCheaperClass/              # 特质: 迅捷精通
│   ├── CheaperClass.cs                # 特质定义（反射修改升级费用）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_cheaperclass.png
│   └── BadNorthCheaperClass.csproj
│
├── BadNorthRegenerative/              # 特质: 追猎
│   ├── Regenerative.cs               # 特质定义（双刀改造/护盾移除）
│   ├── RegenerativeBuff.cs           # 辅助组件
│   ├── RegenerativeJumpResponder.cs  # 辅助组件
│   ├── RegenerativeSpearBuff.cs      # 辅助组件
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_regenerative.png
│   └── BadNorthRegenerative.csproj
│
├── BadNorthThorns/                    # 特质: 荆棘反弹
│   ├── Thorns.cs                      # 特质定义（IAttackResponder 反伤）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_thorns.png
│   └── BadNorthThorns.csproj
│
├── BadNorthFlyer/                     # 特质: 神鹰
│   ├── Flyer.cs                       # 特质定义（英雄飞斧 + 全队击飞效果）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_flyer.png
│   └── BadNorthFlyer.csproj
│
├── BadNorthTitan/                     # 特质: 泰坦
│   ├── Titan.cs                       # 特质定义（高属性巨人化，升级后起效）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_titan.png
│   └── BadNorthTitan.csproj
│
├── BadNorthUltimateSquad/             # 特质: 终极部队
│   ├── UltimateSquad.cs               # 特质定义（英雄坦克化+小兵全面强化，IAttackResponder免疫击退）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_ultimatesquad.png
│   └── BadNorthUltimateSquad.csproj
│
├── BadNorthYuri/                      # 特质: 心灵精英
│   ├── MindElite.cs                   # 特质定义（英雄周期性心灵活性技能）
│   ├── YuriComponent.cs               # 心灵冲击组件（按等级周期释放伤害+眩晕）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_yuri.png
│   └── BadNorthYuri.csproj
│
└── BadNorthSlash/                     # 特质: 横扫之刃
    ├── SweepingBlade.cs               # 特质定义（英雄高抗晕，小兵获得 SlashSword）
    ├── SlashSword.cs                  # 横扫组件（满血+0.4m范围内最多2个额外目标溅射伤害）
    ├── Plugin.cs                      # Mod 入口
    ├── Properties/AssemblyInfo.cs
    ├── Resources/trait_slash.png
    └── BadNorthSlash.csproj
```

## 依赖关系

```
BadNorthAxeThrower.dll ────┐
BadNorthCheaperClass.dll ──┤
BadNorthRegenerative.dll ──┤
BadNorthThorns.dll ────────┤  ──→ BadNorthAPI.dll ──→ MMHOOK-Assembly-CSharp.dll ──→ BepInEx.dll ──→ Assembly-CSharp.dll
BadNorthFlyer.dll ─────────┤
BadNorthTitan.dll ─────────┤
BadNorthUltimateSquad.dll ─┤
BadNorthYuri.dll ──────────┤
BadNorthSlash.dll ─────────┘
```

- **强依赖链**: 每个 Mod → BadNorthAPI → MMHOOK → BepInEx → 游戏程序集
- **同级无依赖**: 9 个特质 Mod 之间互不引用，完全独立
- **零侵入**: 所有运行时修改通过 MMHOOK 委托实现，不修改任何原始游戏文件

## 命名方案（v2 模块化重构）

| 项目 | 原值 | 新值 |
|------|------|------|
| BepInPlugin GUID | nacu.bnapi | nacu.bnapi.modular |
| DLL 文件名 | BNAPI.dll | BadNorthAPI.dll |
| 显示名称 | BN Mod API | BN Mod API (Modular) |
| 命名空间 | BNAPI | BadNorthAPI |
| 依赖字符串 | nacu.bnapi | nacu.bnapi.modular |

## 特质 ID 一览

| 特质 | ID | 命名空间 | GUID |
|------|-----|---------|------|
| 掷斧手 | `Hero_Trait_AxeThrower` | BadNorthAxeThrower | nacu.badnorthaxethrower |
| 迅捷精通 | `Hero_Trait_CheaperClass` | BadNorthCheaperClass | nacu.badnorthcheaperclass |
| 追猎 | `Hero_Trait_Regenerative` | BadNorthRegenerative | nacu.badnorthregenerative |
| 荆棘 | `Hero_Trait_Thorns` | BadNorthThorns | nacu.badnorththorns |
| 神鹰 | `Hero_Trait_Flyer` | BadNorthFlyer | nacu.badnorthflyer |
| 泰坦 | `Hero_Trait_Titan` | BadNorthTitan | nacu.badnorthtitan |
| 终极部队 | `Hero_Trait_UltimateSquad` | BadNorthUltimateSquad | nacu.badnorthultimatesquad |
| 心灵精英 | `Hero_Trait_Yuri` | BadNorthYuri | nacu.badnorthyuri |
| 横扫之刃 | `Hero_Trait_Slash` | BadNorthSweepingBlade | nacu.badnorthsweepingblade |

## 构建说明

### 前置条件

- .NET Framework 4.8 SDK
- 外部引用路径（相对 csproj 所在目录的项目根）:
  - `Assembly-CSharp`: `..\..\..\BadNorth\BadNorth_Data\Managed\`
  - `BepInEx`: `..\..\..\BadNorth\BepInEx\core\`
  - `MMHOOK-Assembly-CSharp`: `..\..\..\BadNorth\BepInEx\plugins\`
  - `UnityEngine.*`: `..\..\..\BadNorth\BadNorth_Data\Managed\`
  - `BadNorthAPI.dll`: `..\BadNorthAPI\BNAPI\bin\Debug\BadNorthAPI.dll`（先构建 BadNorthAPI）

### 手动分步构建

```powershell
dotnet build BadNorthAPI/BNAPI/BNAPI.csproj -c Debug
dotnet build BadNorthAxeThrower/BadNorthAxeThrower.csproj -c Debug
dotnet build BadNorthCheaperClass/BadNorthCheaperClass.csproj -c Debug
dotnet build BadNorthRegenerative/BadNorthRegenerative.csproj -c Debug
dotnet build BadNorthThorns/BadNorthThorns.csproj -c Debug
dotnet build BadNorthFlyer/BadNorthFlyer.csproj -c Debug
dotnet build BadNorthTitan/BadNorthTitan.csproj -c Debug
dotnet build BadNorthUltimateSquad/BadNorthUltimateSquad.csproj -c Debug
dotnet build BadNorthYuri/BadNorthYuri.csproj -c Debug
dotnet build BadNorthSlash/BadNorthSlash.csproj -c Debug
```

### 输出产物

| 项目 | DLL 路径 |
|------|----------|
| BadNorthAPI | `BadNorthAPI/BNAPI/bin/Debug/BadNorthAPI.dll` |
| AxeThrower | `BadNorthAxeThrower/bin/Debug/BadNorthAxeThrower.dll` |
| CheaperClass | `BadNorthCheaperClass/bin/Debug/BadNorthCheaperClass.dll` |
| Regenerative | `BadNorthRegenerative/bin/Debug/BadNorthRegenerative.dll` |
| Thorns | `BadNorthThorns/bin/Debug/BadNorthThorns.dll` |
| Flyer | `BadNorthFlyer/bin/Debug/BadNorthFlyer.dll` |
| Titan | `BadNorthTitan/bin/Debug/BadNorthTitan.dll` |
| UltimateSquad | `BadNorthUltimateSquad/bin/Debug/BadNorthUltimateSquad.dll` |
| Yuri | `BadNorthYuri/bin/Debug/BadNorthYuri.dll` |
| Slash | `BadNorthSlash/bin/Debug/BadNorthSlash.dll` |

## 安装说明（游戏部署）

将构建产物复制到游戏 BepInEx 插件目录：

```
BadNorth/BepInEx/plugins/
├── BadNorthAPI.dll                    # 必需前置
├── BadNorthAxeThrower.dll             # 可选 - 掷斧手特质
├── BadNorthCheaperClass.dll           # 可选 - 迅捷精通特质
├── BadNorthRegenerative.dll           # 可选 - 追猎特质
├── BadNorthThorns.dll                 # 可选 - 荆棘反弹特质
├── BadNorthFlyer.dll                  # 可选 - 神鹰特质
├── BadNorthTitan.dll                  # 可选 - 泰坦特质
├── BadNorthUltimateSquad.dll          # 可选 - 终极部队特质
├── BadNorthYuri.dll                   # 可选 - 心灵精英特质
├── BadNorthSlash.dll                  # 可选 - 横扫之刃特质
```

> 每个 Mod 的 `Resources/trait_xxx.png` 图标文件会自动复制到 DLL 同目录，无需手动操作。

---

## 代码注意事项（必读）

本章节汇总了所有源代码中需要特别注意的模式、陷阱和规范。

### 1️⃣ 通用编码规范

#### 1.1 Mono 2.0 兼容性 —— 使用 `ReferenceEquals` 替代 `!= null`

> **⚠️ 关键规则：** 所有对 `System.Reflection` 类型（`FieldInfo`、`MethodInfo`、`ConstructorInfo` 等）的 null 判断，**必须**使用 `ReferenceEquals`，**禁止**使用 `!=` 或 `==` 运算符。

**原因**：Mono 2.0 运行时下，`FieldInfo.op_Inequality` 和 `FieldInfo.op_Equality` 可能缺失，直接使用 `!= null` 会导致 `MissingMethodException`。

```csharp
// ✅ 正确
if (!ReferenceEquals(field, null))
    field.SetValue(instance, value);

// ❌ 错误（可能在 Mono 2.0 崩溃）
if (field != null)
    field.SetValue(instance, value);
```

**适用范围**：`FieldInfo`、`MethodInfo`、`PropertyInfo`、`ConstructorInfo`、`ParameterInfo`，以及所有非确定性 null 判断场景。

#### 1.2 反射字段访问 —— 使用非泛型包装方法

每个需要反射访问游戏内部私有字段的特质，应定义专用的非泛型包装方法：

```csharp
private static object GetFieldValue(AxeThrowing instance, string fieldName)
{
    FieldInfo field = typeof(AxeThrowing).GetField(fieldName,
        BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public);
    if (ReferenceEquals(field, null))
    {
        Plugin.Logger.LogWarning(string.Format("[Xxx] 反射字段 {0} 未找到", fieldName));
        return null;
    }
    return field.GetValue(instance);
}
```

**优点**：避免泛型导致的类型转换错误，统一处理字段查找失败。

#### 1.3 所有 Plugin 使用相同 Logger 模式

```csharp
public static ManualLogSource Logger;

public void OnEnable()
{
    Logger = base.Logger;
    // ...
}
```

#### 1.4 Plugin 元数据规范

```csharp
[BepInDependency("nacu.bnapi.modular", BepInDependency.DependencyFlags.HardDependency)]
[BepInPlugin("nacu.badnorthxxx", "Bad North - Xxx Trait", "1.0")]
```

- GUID 格式：`nacu.badnorth{特质英文小写}`
- 显示名称格式：`Bad North - Xxx Trait`
- 每个 Mod 必须在 `[BepInDependency]` 中声明对 BadNorthAPI 的硬依赖

#### 1.5 图标命名与引用规范

- 图标文件路径：`Resources/trait_{英文名}.png`
- 在 Plugin.cs 中加载：`CustomSprites.AddCustomSprite(modPath, "trait_{英文名}");`
- 在特质定义构造函数中引用：`this.infoSprite = CustomSprites.Sprites["trait_{英文名}"];`

### 2️⃣ BadNorthFlyer —— 神鹰特质

`BadNorthFlyer/Flyer.cs` 将原 PlentyTraits Flyer 重写为模块化版本：
- 英雄获得飞斧投掷能力（三级容错获取 AxeThrowing 模板，继承自 AxeThrower）
- 全队获得击飞效果（通过反射设置 Wannafly/spearfly/archerfly 字段）
- 英雄替换为双刀维京动画控制器（CopyTwoHandedAnimator）
- 小兵减速至 2.5 移速
- 单一警告输出：`_wannaFlyWarningLogged` 控制 Swordsman.Wannafly 字段未找到时仅警告一次

### 3️⃣ BadNorthTitan —— 泰坦特质

`BadNorthTitan/Titan.cs` 将原 PlentyTraits Titan 重写为模块化版本：
- 需要小队等级 >= 1 才能生效（`squad.level < 1` 时返回）
- 步兵和弓箭手皆可泰坦化：提升体型、伤害、护甲、抗性
- 小队人数减半（`squad.maxCount = squad.maxCount / 2 + 1`）
- 通过反射安全设置 `Armor.armor`（浮点数组）和 `Stun.stunMultiplier`（1E-06f 免疫眩晕）
- 弓箭手从 Viking_TankArcher 获取模板（箭矢+声音）
- 通过反射遍历 `_archerySettings` 修改冷却/散布/攻击属性

### 4️⃣ BadNorthUltimateSquad —— 终极部队特质

还原 `FancyTraits/UltimateSquad.cs`：
- 英雄坦克化改造：复制 `Viking_Tank` 的 Animator，`scale = 1.28`，护甲 `{6,8,11,14}`，伤害 ×2.5，击退 ×5，眩晕 ×2，`stunMultiplier = 0.1`
- 英雄实现 `IAttackResponder`，`ModifyAttack` 中将 `attack.knockback = 0f`（免疫击退）
- `OnPurchased` 中：`hero.maxUsesPerTurn += 1`，购买折扣 `-0.5f`（Class 类型）
- 小兵温和全面强化：体型 ×1.1，血量 ×1.8，速度 ×1.25，`stunMultiplier = 0.5`
  - Swordsman：伤害 ×1.3，击退/眩晕 ×1.5，动画速度 ×1.1，坦克音效
  - Archery：伤害 ×1.2，眩晕 ×1.3，击退 ×1.4，散布 ×0.5，蓄力 ×0.6，冷却 ×0.8
  - Spear：枪长 ×1.3，伤害 ×1.5，眩晕 ×1.5，击退 ×1.5

### 5️⃣ BadNorthYuri —— 心灵精英特质

还原 `FancyTraits/Yuri.cs` + `YuriComponent.cs`：
- `MindElite` 为英雄添加 `YuriComponent`（`PSIswitch = true`），英雄 `stunMultiplier = 0.1f`
- `YuriComponent` 按等级周期释放心灵冲击：
  - 等级对应冷却：`{3.5, 2.8, 2.2, 1.8}` 秒
  - 等级对应范围：`{2, 3, 4, 4.5}` 米
  - 等级对应伤害+眩晕：`(1/4), (1.5/6), (2/8), (2.5/30)`
  - 活人 ≤6 时冷却 ×0.8；`PSImdf`（SpeedUp 道具）使冷却 ×0.5
  - 播放 `plungeLandEffect` + `bloodSplash` 特效
  - 对 `enemyAgent` 造成伤害，`monoAttacker = this`

### 6️⃣ BadNorthSlash —— 横扫之刃特质

还原 `FancyTraits/Slash.cs` + `SlashSword.cs`：
- `SweepingBlade`：英雄 `stunMultiplier = 0.1f`，添加 `SlashSword`；若 `squad.level ≥ 1` 且小兵为 Swordsman，则为小兵添加 `SlashSword`
- `SlashSword` (AgentComponent)：Swordsman 攻击激活后，在 pursuing/ready/hunting 状态下触发：
  - 先回满血量：`base.agent.health = base.agent.maxHealth`
  - 对 0.4m 内的敌方 Agent（`GetStaticListRadiusSorted`）进行溅射
  - 跳过主目标 `component.target`，最多额外攻击 2 个目标
  - 溅射逐次衰减：击退 ×0.65，眩晕 ×0.8，伤害 ×0.8
  - `attack.soundPrefix = ""`，`attack.monoAttacker = this`

### 7️⃣ 五个新特质数值详情

以下罗列五个新特质的完整数值参数（直接提取自源代码常量）。

#### 7.1 神鹰 (Flyer) — `BadNorthFlyer/Flyer.cs`

| 类别 | 参数 | 值 |
|------|------|-----|
| 飞斧（英雄） | 伤害倍率 | ×0.5 |
| | 发射冲量 | 9 |
| | 弹药量 | 500 |
| 击飞（英雄） | maxSpeed | 8 |
| | 击退等级索引 | 3 |
| | 击退倍率 | ×2 |
| | Wannafly（击飞力） | 5 |
| 小兵 | maxSpeed（减速） | 2.5 |
| | 步兵击飞力 | 5 |
| | 矛兵击飞力 | 4 |
| | 弓箭手击飞力 | 3 |
| 英雄动画 | 替换为双刀维京样式 | `Viking_Twohanded` |

#### 7.2 泰坦 (Titan) — `BadNorthTitan/Titan.cs`

| 类别 | 参数 | 值 |
|------|------|-----|
| 条件 | 最低小队等级 | 1 |
| 通用 | 体形缩放 | ×1.25 |
| | 眩晕倍率 | 1E-06（几乎免疫） |
| | 小队人数公式 | `maxCount / 2 + 1` |
| 步兵 | 伤害倍率 | ×2 |
| | 击退倍率 | ×1.5 |
| | 眩晕倍率 | ×1.5 |
| | maxSpeed | 3 |
| | 护甲等级 | {3, 5, 7, 8} |
| 弓箭手 | maxSpeed | 2.5 |
| | 冷却倍率 | ×1.3 |
| | 散布倍率 | ×0.4 |
| | 伤害倍率 | ×1.5 |
| | 击退倍率 | ×1.1 |
| | 眩晕倍率 | ×1.1 |
| | 护甲等级 | {2, 3, 4, 5} |
| | 模板来源 | `Viking_TankArcher`（箭矢+音效） |

#### 7.3 终极部队 (UltimateSquad) — `BadNorthUltimateSquad/UltimateSquad.cs`

还原 `FancyTraits/UltimateSquad.cs`。

**英雄坦克化（HeroEffect）**：

| 参数 | 值 |
|------|-----|
| 动画控制器 | 复制 `Viking_Tank`（CopyTank） |
| 体形缩放 (scale) | 1.28 |
| maxSpeed 倍率 | ×1.3 |
| 动画速度倍率 | ×1.125 |
| 伤害倍率 (damageLevels) | ×2.5 |
| 击退倍率 (knockbackLevels) | ×5 |
| 眩晕倍率 (stunLevels) | ×2 |
| 护甲 (armor) | {6, 8, 11, 14} |
| 眩晕抗性 (stunMultiplier) | 0.1 |
| 音效 (swingSound / swordSound / baseMoveSoundRef) | 来自 `Viking_Tank.vikingClone` |
| 击退免疫 | `ModifyAttack` 中 `attack.knockback = 0f` |

**购买效果（OnPurchased）**：

| 参数 | 值 |
|------|-----|
| maxUsesPerTurn | +1 |
| discount | -0.5 |
| discountType | Class |

**小兵强化（UltimateEffect）**：

| 类别 | 参数 | 值 |
|------|------|-----|
| 通用 | 体形缩放 | ×1.1 |
| | 生命倍率 | ×1.8 |
| | maxSpeed 倍率 | ×1.25 |
| | 眩晕抗性 | 0.5 |
| | 死亡音效 | `Sfx/Viking/Twohanded/Die` |
| | 移动音效 | 来自 `Viking_Tank.vikingClone` |
| 步兵 (Swordsman) | 动画速度 | ×1.1 |
| | 伤害倍率 | ×1.3 |
| | 击退倍率 | ×1.5 |
| | 眩晕倍率 | ×1.5 |
| | 音效 | 坦克风格 (Tank) |
| 弓箭手 (Archery) | 伤害倍率 | ×1.2 |
| | 眩晕倍率 | ×1.3 |
| | 击退倍率 | ×1.4 |
| | 散布倍率 | ×0.5 |
| | 蓄力倍率 | ×0.6 |
| | 冷却倍率 | ×0.8 |
| 矛兵 (Spear) | 枪长倍率 | ×1.3 |
| | 伤害倍率 | ×1.5 |
| | 眩晕倍率 | ×1.5 |
| | 击退倍率 | ×1.5 |
| | 移除 attackResponders | 移除自身 |

#### 7.4 心灵精英 (Yuri) — `BadNorthYuri/MindElite.cs` + `YuriComponent.cs`

还原 `FancyTraits/Yuri.cs` + `YuriComponent.cs`。

**英雄设置（OnAppliedToSquad）**：

| 参数 | 值 |
|------|-----|
| YuriComponent | GetOrAddComponent，PSIswitch = true |
| stunMultiplier | 0.1（高抗性） |

**心灵冲击数值（YuriComponent 按等级索引）**：

| 等级 | 冷却 | 范围 | 伤害 | 击退 | 眩晕 |
|------|------|------|------|------|------|
| Lv0 (1) | 3.5s | 2m | 1 | 5 | 4 |
| Lv1 (2) | 2.8s | 3m | 1.5 | 6 | 6 |
| Lv2 (3) | 2.2s | 4m | 2 | 7 | 8 |
| Lv3 (4) | 1.8s | 4.5m | 2.5 | 8 | 30 |

**冷却加速条件**：`livingAgents.Count ≤ 6` → 冷却 ×0.8；`PSImdf = true` → 冷却 ×0.5

**技能触发**：当 `enemyDist ≤ psirange[level]` 且 `enemyAgent` 不为空时触发，对 `enemyAgent` 造成伤害

**特效**：`plungeLandEffect` + `bloodSplash`

#### 7.5 横扫之刃 (Slash) — `BadNorthSlash/SweepingBlade.cs` + `SlashSword.cs`

还原 `FancyTraits/Slash.cs` + `SlashSword.cs`。

**英雄设置（OnAppliedToSquad）**：

| 参数 | 值 |
|------|-----|
| stunMultiplier | 0.1 |
| SlashSword | GetOrAddComponent |
| 小兵 SlashSword | squad.level ≥ 1 且 Swordsman 存在时添加 |

**SlashSword 触发逻辑（Update）**：

| 条件 | 行为 |
|------|------|
| `component.attack.active` | 设置 `Slashready = true` |
| `pursuing.active \|\| ready.active \|\| hunting.active` 且 `Slashready` | 触发横扫 |

**横扫效果**：

| 参数 | 值 |
|------|-----|
| 回血 | `agent.health = agent.maxHealth`（满血） |
| 搜索范围 | 0.4m（`GetStaticListRadiusSorted`） |
| 最大额外目标 | 2 |
| 跳过目标 | `component.target` |
| 第1次溅射衰减 | 无衰减（使用原始 GetAttack 值） |
| 第2次溅射衰减 | 伤害 ×0.8，击退 ×0.65，眩晕 ×0.8 |
| attack.soundPrefix | `""`（空字符串） |
| attack.monoAttacker | `this` |

### 8️⃣ 构建与部署常见问题

#### 8.1 构建顺序

必须先构建 BadNorthAPI 生成 `BadNorthAPI.dll`，再构建 9 个特质 Mod。

#### 8.2 csproj HintPath 层级

所有外部引用路径相对于 csproj 文件所在目录：

```xml
<!-- BadNorthAxeThrower/BadNorthAxeThrower.csproj 中的路径 -->
<HintPath>..\..\..\BadNorth\BadNorth_Data\Managed\Assembly-CSharp.dll</HintPath>
          ↑  ↑  ↑
          退3级到 BadNorthProgram 根目录，再进入 BadNorth 游戏目录

<HintPath>..\BadNorthAPI\BNAPI\bin\Debug\BadNorthAPI.dll</HintPath>
          ↑
          退1级到 BadNorthHeroTraits 根目录，再进入 BadNorthAPI 项目
```

**注意**：BadNorthAPI.csproj 中的外部引用也使用了 `..\..\..\BadNorth\` 的相对路径（从 `BadNorthAPI/BNAPI/` 退3级到 `BadNorthProgram` 根目录）。

### 9️⃣ 扩展新特质指南

1. 复制任一现有特质 Mod 目录（推荐复制 `BadNorthThorns`，最轻量）
2. 修改 `Plugin.cs`：
   - GUID、显示名称
   - 图标文件名、注册的特质 ID
   - 本地化文本术语
3. 编写新的 `HeroUpgradeDefinition` 子类，实现 `OnAppliedToSquad`
4. 准备 `Resources/trait_xxx.png` 图标（建议 128x128 PNG）
5. 在构造函数的 `infoSprite` 中引用图标
6. 如果需要添加组件：使用 `GetOrAddComponent<T>` 扩展方法
7. 如果需要访问私有字段：实现专用的 `GetFieldValue`/`SetFieldValue` 反射方法（注意 `ReferenceEquals`）
8. 如果实现 `IAttackResponder`：注意通过 `squad.onAgentCreated` 添加到 `agent.attackResponders`
9. 编译测试