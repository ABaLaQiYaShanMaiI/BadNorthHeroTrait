# BadNorthHeroTraits 架构方案

## 概述

将原本单 DLL 多特质的 PlentyTraits Mod 拆分为 4 个独立的特质 Mod + 1 个公共 API 库 (BadNorthAPI)。每个 Mod 遵循 **单一职责** 原则，仅负责一个特质的注册与运行逻辑。

## 项目结构（当前实际）

```
BadNorthHeroTraits/                    # 根目录（Git 仓库）
├── build_all.ps1                      # 一键构建脚本
├── 架构方案.md                         # 本文件
│
├── BNAPI/                             # 公共 API 层（前置依赖）
│   └── BNAPI/
│       ├── CustomSprites.cs           # 精灵/图标加载
│       ├── CustomText.cs              # 本地化术语注入
│       ├── CustomTraits.cs            # 特质注册
│       ├── IgnoresAccessChecksToAttribute.cs
│       ├── Debugger.cs                # 调试日志工具
│       ├── Plugin.cs                  # BNAPI 入口（GUID: nacu.bnapi.modular）
│       ├── Properties/AssemblyInfo.cs
│       └── BNAPI.csproj (AssemblyName: BadNorthAPI)
│
├── BadNorthAxeThrower/                # 特质: 掷斧手
│   ├── AxeThrower.cs                  # 特质定义（三级容错获取模板）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_axe.png        # 特质图标
│   └── BadNorthAxeThrower.csproj
│
├── BadNorthCheaperClass/              # 特质: 迅捷精通
│   ├── CheaperClass.cs                # 特质定义（反射修改升级费用）
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_cheaperclass.png
│   └── BadNorthCheaperClass.csproj
│
├── BadNorthRegenerative/              # 特质: 追猎
│   ├── Regenerative.cs               # 特质定义（双刀改造/护盾移除）
│   ├── RegenerativeBuff.cs           # 辅助组件
│   ├── RegenerativeJumpResponder.cs  # 辅助组件
│   ├── RegenerativeSpearBuff.cs      # 辅助组件
│   ├── Plugin.cs                      # Mod 入口
│   ├── Properties/AssemblyInfo.cs
│   ├── Resources/trait_regenerative.png
│   └── BadNorthRegenerative.csproj
│
└── BadNorthThorns/                    # 特质: 荆棘反弹
    ├── Thorns.cs                      # 特质定义（IAttackResponder 反伤）
    ├── Plugin.cs                      # Mod 入口
    ├── Properties/AssemblyInfo.cs
    ├── Resources/trait_thorns.png
    └── BadNorthThorns.csproj
```

## 依赖关系

```
BadNorthAxeThrower.dll ─┐
BadNorthCheaperClass.dll ─┤
BadNorthRegenerative.dll ─┤  ──→ BadNorthAPI.dll ──→ MMHOOK-Assembly-CSharp.dll ──→ BepInEx.dll ──→ Assembly-CSharp.dll
BadNorthThorns.dll ──────┘
```

- **强依赖链**: 每个 Mod → BadNorthAPI → MMHOOK → BepInEx → 游戏程序集
- **同级无依赖**: 4 个特质 Mod 之间互不引用，完全独立
- **零侵入**: 所有运行时修改通过 MMHOOK 委托实现，不修改任何原始游戏文件

## 命名方案（v2 模块化重构）

| 项目 | 原值 | 新值 |
|------|------|------|
| BepInPlugin GUID | nacu.bnapi | nacu.bnapi.modular |
| DLL 文件名 | BNAPI.dll | BadNorthAPI.dll |
| 显示名称 | BN Mod API | BN Mod API (Modular) |
| 命名空间 | BNAPI | BadNorthAPI |
| 依赖字符串 | nacu.bnapi | nacu.bnapi.modular |

## 构建说明

### 前置条件

- .NET Framework 4.8 SDK
- 外部引用路径（相对 csproj 所在目录的项目根）:
  - `Assembly-CSharp`: `..\..\..\BadNorth\BadNorth_Data\Managed\`
  - `BepInEx`: `..\..\..\BadNorth\BepInEx\core\`
  - `MMHOOK-Assembly-CSharp`: `..\..\..\BadNorth\BepInEx\plugins\`
  - `UnityEngine.*`: `..\..\..\BadNorth\BadNorth_Data\Managed\`
  - `BadNorthAPI.dll`: `..\BNAPI\BNAPI\bin\Debug\BadNorthAPI.dll`（先构建 BNAPI）

### 一键构建

```powershell
.\build_all.ps1
```

### 手动分步构建

```powershell
dotnet build BNAPI/BNAPI/BNAPI.csproj -c Debug
dotnet build BadNorthAxeThrower/BadNorthAxeThrower.csproj -c Debug
dotnet build BadNorthCheaperClass/BadNorthCheaperClass.csproj -c Debug
dotnet build BadNorthRegenerative/BadNorthRegenerative.csproj -c Debug
dotnet build BadNorthThorns/BadNorthThorns.csproj -c Debug
```

### 输出产物

| 项目 | DLL 路径 |
|------|----------|
| BadNorthAPI | `BNAPI/BNAPI/bin/Debug/BadNorthAPI.dll` |
| AxeThrower | `BadNorthAxeThrower/bin/Debug/BadNorthAxeThrower.dll` |
| CheaperClass | `BadNorthCheaperClass/bin/Debug/BadNorthCheaperClass.dll` |
| Regenerative | `BadNorthRegenerative/bin/Debug/BadNorthRegenerative.dll` |
| Thorns | `BadNorthThorns/bin/Debug/BadNorthThorns.dll` |

## 安装说明（游戏部署）

将构建产物复制到游戏 BepInEx 插件目录：

```
BadNorth/BepInEx/plugins/
├── BadNorthAPI.dll                    # 必需前置
├── BadNorthAxeThrower.dll             # 可选 - 掷斧手特质
├── BadNorthCheaperClass.dll           # 可选 - 迅捷精通特质
├── BadNorthRegenerative.dll           # 可选 - 追猎特质
├── BadNorthThorns.dll                 # 可选 - 荆棘反弹特质
```

> 每个 Mod 的 `Resources/trait_xxx.png` 图标文件会自动复制到 DLL 同目录，无需手动操作。

---

## 代码注意事项（必读）

本章节汇总了所有源代码中需要特别注意的模式、陷阱和规范。

### 1️⃣ 通用编码规范

#### 1.1 Mono 2.0 兼容性 —— 使用 `ReferenceEquals` 替代 `!= null`

> **⚠️ 关键规则：** 所有对 `System.Reflection` 类型（`FieldInfo`、`MethodInfo`、`ConstructorInfo` 等）的 null 判断，**必须**使用 `ReferenceEquals`，**禁止**使用 `!=` 或 `==` 运算符。

**原因**：Mono 2.0 运行时下，`FieldInfo.op_Inequality` 和 `FieldInfo.op_Equality` 可能缺失，直接使用 `!= null` 会导致 `MissingMethodException`。

```csharp
// ✅ 正确
if (!ReferenceEquals(field, null))
    field.SetValue(instance, value);

// ❌ 错误（可能在 Mono 2.0 崩溃）
if (field != null)
    field.SetValue(instance, value);
```

**适用范围**：`FieldInfo`、`MethodInfo`、`PropertyInfo`、`ConstructorInfo`、`ParameterInfo`，以及所有非确定性 null 判断场景。

#### 1.2 反射字段访问 —— 使用非泛型包装方法

每个需要反射访问游戏内部私有字段的特质，应定义专用的非泛型包装方法：

```csharp
private static object GetFieldValue(AxeThrowing instance, string fieldName)
{
    FieldInfo field = typeof(AxeThrowing).GetField(fieldName,
        BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public);
    if (ReferenceEquals(field, null))
    {
        Plugin.Logger.LogWarning($"[Xxx] 反射字段 {fieldName} 未找到");
        return null;
    }
    return field.GetValue(instance);
}

private static void SetFieldValue(AxeThrowing instance, string fieldName, object value)
{
    FieldInfo field = typeof(AxeThrowing).GetField(fieldName,
        BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public);
    if (ReferenceEquals(field, null))
    {
        Plugin.Logger.LogWarning($"[Xxx] 反射字段 {fieldName} 未找到，无法设置");
        return;
    }
    field.SetValue(instance, value);
}
```

**优点**：避免泛型导致的类型转换错误，统一处理字段查找失败。

#### 1.3 所有 Plugin 使用相同 Logger 模式

```csharp
public static ManualLogSource Logger;

public void OnEnable()
{
    Logger = base.Logger;
    // ...
}
```

#### 1.4 Plugin 元数据规范

```csharp
[BepInDependency("nacu.bnapi.modular", BepInDependency.DependencyFlags.HardDependency)]
[BepInPlugin("nacu.badnorthxxx", "Bad North - Xxx Trait", "1.0")]
```

- GUID 格式：`nacu.badnorth{特质英文小写}`
- 显示名称格式：`Bad North - Xxx Trait`
- 每个 Mod 必须在 `[BepInDependency]` 中声明对 BadNorthAPI 的硬依赖

#### 1.5 图标命名与引用规范

- 图标文件路径：`Resources/trait_{英文名}.png`
- 在 Plugin.cs 中加载：`CustomSprites.AddCustomSprite(modPath, "trait_{英文名}");`
- 在特质定义构造函数中引用：`this.infoSprite = CustomSprites.Sprites["trait_{英文名}"];`

### 2️⃣ BadNorthAPI 注意事项

#### 2.1 CustomTraits —— 反射动态适配 MetaInventory.UpgradeEntry 构造函数

`CustomTraits.cs` 使用纯反射访问 `MetaInventory.UpgradeEntry`，因为该类型是内部嵌套类型，不同游戏版本可能有不同构造函数签名。

核心逻辑：
1. 遍历 `UpgradeEntry` 的所有构造函数，自动匹配可用签名
2. 支持 2 参数 `(HeroUpgradeDefinition, bool/int/object)` 和 3 参数 `(HeroUpgradeDefinition, int, bool)`
3. 若找不到构造函数，设置 `_reflectionFailed = true`，跳过所有自定义特质注册（不影响游戏启动）

**重要**：添加新游戏版本支持时，可能需要在此处增加新的构造函数适配逻辑。

#### 2.2 CustomText —— 术语重复注册保护

`CustomText.AddCustomTerm()` 内置重复检查：

```csharp
if (source.ContainsTerm(term))
{
    Plugin.logger.LogInfo($"TERM \"{term}\" 已存在，跳过注册");
    return;
}
```

当多个 Mod 注册相同术语时，不会崩溃，只会记录 Info 日志。

### 3️⃣ BadNorthAxeThrower —— 三级容错获取模板

#### 3.1 `GetAxeThrowingTemplate()` 容错策略

由于游戏版本差异可能导致 `LevelStateObjectReferences.dict` 中的键名或结构变更，AxeThrower 实现了三级容错：

| 级别 | 方法 | 说明 |
|------|------|------|
| 1 | `LevelStateObjectReferences.dict["Viking_AxeThrower"]` | 精确查找，通过反射访问 viking/vikingClone 字段 |
| 2 | `Resources.FindObjectsOfTypeAll<AxeThrowing>()` | 遍历场景中所有 AxeThrowing 组件 |
| 3 | 返回 `null` | 均失败时使用攻击设置的默认值 |

#### 3.2 FieldInfo 反射注意事项

`AxeThrower.cs` 中通过字符串常量定义私有字段名：

```csharp
private const string FIELD_PREPARE_SOUND = "prepareSound";
private const string FIELD_THROWING_AXE_PREFAB = "throwingAxePrefab";
// ...
```

若游戏版本更新导致 `AxeThrowing` 字段名变更，此处需同步更新。

### 4️⃣ BadNorthCheaperClass —— 折扣应用与防重复

#### 4.1 双重防重复机制

```csharp
private bool discountApplied = false;          // 实例级
private static bool _globalDiscountApplied = false;  // 全局级
```

- **实例级**: 防止同一个 CheaperClass 实例重复应用到同一小队
- **全局级**: 防止多个 CheaperClass 实例（或多个特质 Mod）重复折扣

#### 4.2 构造时提前触发 + OnAppliedToSquad 双路径

- **构造时**（`TryTriggerGlobalDiscount`）：在 CheaperClass 实例化时立即尝试应用折扣（通过反射查找所有 `HeroDefinition`，绕过 `SpawnSquad MissingMethodException` 问题）
- **应用时**（`OnAppliedToSquad`）：当特质实际应用到小队时再次尝试

#### 4.3 反射遍历 upgrades 列表

`ApplyDiscountToHeroDef` 通过反射遍历 `HeroDefinition.upgrades` 列表，处理两类条目：
- 直接 `HeroUpgradeDefinition` 对象
- 序列化包装类型（含 `definition` 或 `upgrade` 字段 + `levels` 字段）

**注意**：如果游戏版本改变了 `HeroDefinition` 的内部升级字段名或类型结构，此方法可能需要调整。

### 5️⃣ BadNorthRegenerative —— 类型安全与组件处理

#### 5.1 使用字符串类型名避免 JIT TypeLoadException

```csharp
private static readonly string SpearShieldTypeName = "Voxels.TowerDefense.SpearShield";
```

使用 `GameObject.GetComponent(string)` 通过字符串名称引用可能不存在的类型，避免 JIT 编译时因类型不存在而崩溃。

#### 5.2 安全移除组件（禁用而非销毁）

```csharp
private void RemoveShield(Swordsman swordsman, Agent agent)
{
    Shield shield = swordsman.shield;
    if (shield)
    {
        if (shield.shield != null)
        {
            shield.shield.gameObject.SetActive(false);  // ✅ 禁用，不销毁
        }
        shield.enabled = false;
        swordsman.shield = null;
    }
}
```

**原则**：禁用组件及其 GameObject，而非使用 `Object.Destroy()`。避免 `AgentSelected` 等方式访问已销毁渲染器导致 NullReferenceException。

#### 5.3 单一警告输出

```csharp
private static bool _isGiantWarningLogged = false;

if (!_isGiantWarningLogged)
{
    _isGiantWarningLogged = true;
    Plugin.Logger.LogWarning("[Regenerative] Swordsman.isGiant field not found, skipping. (此警告仅显示一次)");
}
```

**原则**：不需要每次运行都输出的警告，使用 `static bool` 开关控制仅输出一次，避免刷屏。

### 6️⃣ BadNorthThorns —— IAttackResponder 接口实现

Thorns 特质直接实现 `IAttackResponder` 接口，通过 `ModifyAttack(ref Attack)` 回调实现反伤：

```csharp
public class Thorns : HeroUpgradeDefinition, IAttackResponder
{
    public void ModifyAttack(ref Attack attack)
    {
        CloseCombatBrain closeCombatBrain = attack.monoAttacker as CloseCombatBrain;
        if (closeCombatBrain != null)
        {
            closeCombatBrain.agent.DealDamage(new Attack(...));
        }
    }
}
```

**注意事项**：
- 添加到 Agent 的 `attackResponders` 列表：通过 `squad.onAgentCreated` 事件
- 只对 `CloseCombatBrain` 类型的攻击者生效（近战反伤）
- 其他攻击类型（弓箭、投掷等）不会触发反伤

### 7️⃣ 构建与部署常见问题

#### 7.1 构建顺序

必须先构建 BNAPI 生成 `BadNorthAPI.dll`，再构建 4 个特质 Mod。`build_all.ps1` 已处理此顺序。

#### 7.2 csproj HintPath 层级

所有外部引用路径相对于 csproj 文件所在目录：

```xml
<!-- BadNorthAxeThrower/BadNorthAxeThrower.csproj 中的路径 -->
<HintPath>..\..\..\BadNorth\BadNorth_Data\Managed\Assembly-CSharp.dll</HintPath>
          ↑  ↑  ↑
          退3级到 BadNorthProgram 根目录，再进入 BadNorth 游戏目录

<HintPath>..\BNAPI\BNAPI\bin\Debug\BadNorthAPI.dll</HintPath>
          ↑
          退1级到 BadNorthHeroTraits 根目录，再进入 BNAPI 项目
```

**注意**：BNAPI.csproj 中的外部引用也使用了 `..\..\..\BadNorth\` 的相对路径（从 BNAPI/BNAPI/ 退3级到 BadNorthProgram 根目录）。

### 8️⃣ 扩展新特质指南

1. 复制任一现有特质 Mod 目录（推荐复制 `BadNorthThorns`，最轻量）
2. 修改 `Plugin.cs`：
   - GUID、显示名称
   - 图标文件名、注册的特质 ID
   - 本地化文本术语
3. 编写新的 `HeroUpgradeDefinition` 子类，实现 `OnAppliedToSquad`
4. 准备 `Resources/trait_xxx.png` 图标（建议 128x128 PNG）
5. 在构造函数的 `infoSprite` 中引用图标
6. 如果需要添加组件：使用 `GetOrAddComponent<T>` 扩展方法
7. 如果需要访问私有字段：实现专用的 `GetFieldValue`/`SetFieldValue` 反射方法（注意 `ReferenceEquals`）
8. 编译测试
